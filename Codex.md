# Pamela Betancourt Compatibility Memory

Start with [AGENTS.md](AGENTS.md), then [README.md](README.md). Read [changelog/README.md](changelog/README.md) only for history.

## Durable decisions

- This repository owns Pamela's public draft JSON and public assets; the Zoolandingpage hub owns shared app, authoring, upload, and publication workflows.
- The hub registry proves `pamelabetancourt.com` as canonical but currently supplies no alias fields. Local alias declarations are not registry approval and must not be inferred or changed incidentally.
- Preserve the configured Sprint de Redes and free diagnosis positioning, intentionally public WhatsApp CTA behavior, and `/contact` Google Form path unless a content task explicitly changes them.
- Upload grants, signed upload URLs, source documents, and investigation notes stay local. Commit only reviewed public assets or stable public asset URLs.
- Work starts from `dev` and promotes through `dev -> test -> main`; `dev` does not deploy.

Do not add chronology here. Keep current behavior in README/config and dated evidence in `changelog/`.
